@model IEnumerable<EF_practice.Models.Event>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NameEvent)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateEvent)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PlaceEvent)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameEvent)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateEvent)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PlaceEvent)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Idevent">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Idevent">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Idevent">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
